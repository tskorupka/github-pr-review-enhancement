{
  "name": "github-pr-review-enhancement",
  "version": "0.4.1",
  "private": true,
  "description": "Chrome extension: collapse multi-line comment blocks in GitHub PR diffs. This package holds dev tooling only (store asset capture).",
  "type": "module",
  "scripts": {
    "capture": "node store/capture.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
